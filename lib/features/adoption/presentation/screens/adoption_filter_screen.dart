// Filter controls are now embedded as a modal bottom sheet in AdoptionHomeScreen.
// This stub is kept in case any external reference remains.
import 'package:flutter/material.dart';

class AdoptionFilterScreen extends StatelessWidget {
  const AdoptionFilterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
